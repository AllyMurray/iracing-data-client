---
title: Client API
description: API reference for the IRacingDataClient class
---

import { Tabs, TabItem, Card, Aside } from '@astrojs/starlight/components';

# Client API

The client provides the core functionality for connecting to the iRacing Data API, handling OAuth2 authentication, and managing requests.

## IRacingDataClient

The main entry point for using the Data Client.

```typescript
import { IRacingDataClient } from 'iracing-data-client';

const iracing = new IRacingDataClient({
  auth: {
    type: 'password-limited',
    clientId: process.env.IRACING_CLIENT_ID,
    clientSecret: process.env.IRACING_CLIENT_SECRET,
    username: process.env.IRACING_USERNAME,
    password: process.env.IRACING_PASSWORD,
  },
});
```

### Constructor Options

```typescript
interface IRacingClientOptions {
  auth: AuthConfig;
  fetchFn?: FetchLike;
}
```

### Auth Config

<Tabs>
  <TabItem label="Password Limited">
    ```typescript
    interface PasswordLimitedAuth {
      type: 'password-limited';
      clientId: string;
      clientSecret: string;
      username: string;
      password: string;
      token?: TokenResponse;        // Pre-obtained token
      onTokenRefresh?: OnTokenRefresh; // Callback on token refresh
    }
    ```
  </TabItem>
  <TabItem label="Authorization Code">
    ```typescript
    interface AuthorizationCodeAuth {
      type: 'authorization-code';
      clientId: string;
      clientSecret: string;
      token: TokenResponse;          // Required: obtained via code exchange
      onTokenRefresh?: OnTokenRefresh; // Callback on token refresh
    }
    ```
  </TabItem>
  <TabItem label="fetchFn">
    **Type:** `FetchLike` (optional)

    Custom fetch implementation for HTTP requests.

    ```typescript
    const iracing = new IRacingDataClient({
      auth: { /* ... */ },
      fetchFn: customFetch,
    });
    ```
  </TabItem>
</Tabs>

### Service Properties

The client provides access to all service classes:

```typescript
const iracing = new IRacingDataClient(options);

iracing.car              // CarService
iracing.carclass         // CarclassService
iracing.constants        // ConstantsService
iracing.driverStatsByCategory  // DriverStatsByCategoryService
iracing.hosted           // HostedService
iracing.league           // LeagueService
iracing.lookup           // LookupService
iracing.member           // MemberService
iracing.results          // ResultsService
iracing.season           // SeasonService
iracing.series           // SeriesService
iracing.stats            // StatsService
iracing.team             // TeamService
iracing.timeAttack       // TimeAttackService
iracing.track            // TrackService
```

## IRacingError

Custom error class for API errors.

```typescript
class IRacingError extends Error {
  status?: number;           // HTTP status code
  statusText?: string;       // HTTP status text
  responseData?: unknown;    // Response body

  // Helper properties
  get isMaintenanceMode(): boolean;  // True if 503 maintenance
  get isRateLimited(): boolean;       // True if 429 rate limit
  get isUnauthorized(): boolean;      // True if 401 unauthorized
}
```

### Error Handling Examples

<Tabs>
  <TabItem label="Basic">
    ```typescript
    try {
      const data = await iracing.track.get();
    } catch (error) {
      if (error instanceof IRacingError) {
        console.error(`API Error: ${error.message}`);
        console.error(`Status: ${error.status}`);
      }
    }
    ```
  </TabItem>
  <TabItem label="Specific Errors">
    ```typescript
    try {
      const data = await iracing.track.get();
    } catch (error) {
      if (error instanceof IRacingError) {
        if (error.isMaintenanceMode) {
          console.log('iRacing is in maintenance mode');
        } else if (error.isRateLimited) {
          console.log('Rate limited - wait before retry');
        } else if (error.isUnauthorized) {
          console.log('Authentication failed');
        }
      }
    }
    ```
  </TabItem>
</Tabs>

## OAuthError

Error class for OAuth-specific failures.

```typescript
class OAuthError extends Error {
  code: string;  // e.g. 'invalid_grant', 'invalid_client', 'rate_limited'
}
```

## TokenRefreshError

Error class for token refresh failures.

```typescript
class TokenRefreshError extends Error {
  cause?: Error;  // The underlying error
}
```

## Type Exports

The SDK exports TypeScript types for all requests and responses:

```typescript
import type {
  // Auth types
  AuthConfig,
  PasswordLimitedAuth,
  AuthorizationCodeAuth,
  TokenResponse,
  OnTokenRefresh,
  FetchLike,

  // Client types
  IRacingClientOptions,
} from 'iracing-data-client';

import {
  IRacingDataClient,
  IRacingError,
  OAuthError,
  TokenRefreshError,
  buildAuthorizationUrl,
  exchangeAuthorizationCode,
} from 'iracing-data-client';
```

## Features

<Card title="Automatic Authentication">
  Handles the complete OAuth2 flow including token acquisition, storage, and automatic refresh.
</Card>

<Card title="S3 Link Following">
  Automatically follows presigned S3 URLs returned by the API for accessing data.
</Card>

<Card title="Response Caching *(coming soon)*">
  Response caching is on the roadmap. See our [roadmap](/roadmap/) for details.
</Card>

<Card title="Parameter Transformation">
  Automatically converts between camelCase (SDK) and snake_case (API) formats.
</Card>

## Performance Considerations

### Rate Limiting

<Aside type="caution">
  The iRacing API has rate limits. The client throws an `IRacingError` with `isRateLimited = true` when limits are exceeded. See the [Rate Limiting guide](/guides/rate-limiting/) for handling strategies.
</Aside>

### Connection Reuse

The client reuses the same authenticated session across all requests:

```typescript
const iracing = new IRacingDataClient(options);

// All requests share the same session and token
await iracing.car.get();
await iracing.track.get();
await iracing.member.get({ custIds: [123456] });
```

## See Also

- [Authentication Guide](/getting-started/authentication/) - Detailed authentication setup
- [Error Handling Guide](/guides/error-handling/) - Comprehensive error handling strategies
- [Services Overview](/api/services/) - All available services
